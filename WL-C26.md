# WL-C26 语法

**Wadell Easy Language Series - Compiled**
**源码**：`.wec`
**头文件**：`.weh`
**产物**：`.wes`

---

## 一、声明

```
变量名 为 类型 值;
```

- 必须给值。空值写 `∅`。
- `为` 是首次声明，已声明过的变量不能再 `为`。

```
a 为 整数 10;
b 为 整数 ∅;
c 为 常量 双精 3.14;
```

**另一种写法**（值在前）：

```
整数 5 为 a;
```

等价于 `a 为 整数 5;`。

**容器**：

```
容器 a;           _创建容器_
5 为 a;           _放值_
"你好" 为 a;      _放其他值_
取 a = b;         _取出来，b 是 a 里的值的副本_
清 a;             _清空容器_
```

**引用声明**：

```
a 为 整数 10;
b 为 引用 a;
```

- 语法：`名 为 引用 目标`
- `b` 的类型自动对应 `a` 的类型
- `b` 是 `a` 的别名，改 `b` 等于改 `a`
- 引用必须在声明时绑定，不能再改绑

---

## 二、类型

**基本类型**：

| 中 | 英 |
|---|---|
| 整数 | int |
| 长整 | long |
| 短整 | short |
| 字节 | byte |
| 无符 | unsigned |
| 浮点 | float |
| 双精 | double |
| 字符 | char |
| 布尔 | bool |
| 空 | void |
| 字符串 | string |

**类型大小**：

| 类型 | 字节 |
|---|---|
| 字节 / 字符 | 1 |
| 短整 | 2 |
| 浮点 | 4 |
| 整数 | 8 |
| 无符 | 8 |
| 长整 | 8 |
| 双精 | 8 |
| 指针 | 8 |

**修饰符**（跟在类型后面，可叠加）：

| 中 | 英 |
|---|---|
| 指针 | ptr |
| 引用 | ref |
| 数组 | array |
| 区间 | range |

**组合**：从左到右层层包裹。

```
整数                 int
整数 指针            int ptr
整数 指针 指针       int ptr ptr
整数 数组            int array
整数 数组 指针       int array ptr
```

**无符组合**：

`无符` 后可跟 `整数` / `长整` / `短整` / `字符`，表示无符号版本：

```
无符 整数     unsigned int
无符 长整     unsigned long
无符 短整     unsigned short
无符 字符     unsigned char
```

**类型别名**：

```
别名 句柄 为 无符 长整 指针;
别名 分数 为 双精;

a 为 句柄 0;
b 为 分数 3.14;
```

- 语法：`别名 新名 为 旧类型;`
- 新名之后可当类型用

**函数指针**：

```
函 加 (a: 整数, b: 整数) -> 整数 { 返 a + b; }

函 主 () -> 整数 {
    f 为 函 指针 0;
    *f = 加;
    x 为 整数 (*f)(1, 2);
    返 0;
}
```

- `函` 和 `函数` 是同一关键字的两种写法
- 声明：`f 为 函 指针 0;`，跟 `p 为 整数 指针 0;` 同构
- 绑定：`*f = 函数名;`，函数名不带括号
- 调用：`(*f)(参数)`，解引用后加括号调用

**布尔值**：

```
真          _1_
假          _0_
```

---

## 三、数组

```
a 为 整数 数组 { 3, 1, 2 };
```

元素用 `{}` 包，逗号分隔。长度 = 元素个数。

---

## 四、区间

```
a 为 整数 区间 (0, 10);
b 为 整数 区间 [0, 10];
c 为 整数 区间 (0, 10];
d 为 整数 区间 [0, 10);
e 为 整数 区间 (0, +∞);
f 为 整数 区间 (-∞, 0);
g 为 整数 区间 (-∞, +∞);
```

- `∞` 只允许在开区间端点
- `[0, +∞]` 非法
- **约束语义**：赋值超出范围 → 忽略，保持原值
- 无初值时打印区间本身

**无穷边界写法：**

- 正无穷：`+∞`
- 负无穷：`-∞`
- `∞` 单独出现非法

**合法：**

```
a 为 整数 区间 (0, +∞);
b 为 整数 区间 (-∞, 0);
c 为 整数 区间 (-∞, +∞);
```

**非法：**

```
x 为 整数 区间 (0, ∞);     _∞ 缺符号_
y 为 整数 区间 [0, +∞];    _闭区间端点不能是 ∞_
```

**规则：**

- `+∞` / `-∞` 只能出现在**开区间端点**
- 即 `(a, +∞)` `(-∞, b)` `(-∞, +∞)` 合法
- `[a, +∞]` `[-∞, b]` 非法

---

## 五、∅ 空值

**运算规则**：

- `∅ + 1` = 1（当 0）
- `∅ - 1` = -1（当 0）
- `∅ * 1` = ∅
- `∅ / 1` = ∅
- `打印(∅)` 非法，要打印 ∅ 字符用字符串 `打印("∅", \n)`
- 变量存 ∅ 后，值当 0 用，打印输出 `0`

---

## 六、结构

```
结构 学生 (字符串 名字, 整数 年龄) {
    学生1 ("张三", 18);
    学生2 ("李四", 20);
};
```

**同类型字段简写**：

```
结构 点 整数 (x, y) {
    点1 (1, 2);
};
```

- 结构必须带变量，变量必须有值
- 字段格式：`类型 名`，逗号分隔
- 定义末尾必须 `;`

**嵌套结构体：**

```
结构 日期 整数 (年, 月, 日) {
    今天 (2026, 9, 25);
};

结构 人 (字符串 名字, 日期 生日) {
    张三 ("张三", 今天);
};

函 主 () -> 整数 {
    打印(张三.生日.年, \n);   _2026_
    返 0;
}
```

- 字段类型可以是另一个结构体
- 字段初值可以引用另一个已定义的实例
- 访问用 `.` 链式：`a.b.c`
- 嵌套字段的数据是**复制**，不是引用（改 `张三.生日` 不影响 `今天`）

---

## 七、联合

```
联合 (变量1, 变量2) (整数 i, 浮点 f) {
    值1 (42) 为 变量3;
};
```

- `(变量1, 变量2)` — 字段名
- `(整数 i, 浮点 f)` — 类型
- `值1 (42) 为 变量3` — 创建实例，值 42，赋给变量3
- 定义末尾必须 `;`

---

## 八、枚举

```
枚举 颜色 {
    红,
    绿,
    蓝
};
```

- 枚举值直接当名字用（`红`，不用 `颜色.红`）
- 无指定则自动编号，从 0 开始
- 有指定用指定值：

```
枚举 颜色 {
    红,          _0_
    绿 = 5,      _5_
    蓝           _6_
};
```

- 定义末尾必须 `;`

---

## 九、函数

```
函 加 (a: 整数, b: 整数) -> 整数 {
    返 a + b;
}
```

或：

```
函 加 (a: 整数, b: 整数) -> 整数
开始
    返 a + b;
结束
```

- 参数格式：`名: 类型`
- 无返回值不写 `->`
- 函数内语句末尾必须 `;`
- **不需要前置声明**（编译器读完整份源码）
- `函` 和 `函数` 是同一关键字的两种写法

**变长参数**：

```

函 求和 (个数: 整数, ...) -> 整数 {
total 为 整数 0;
i 为 整数 0;
循环 i = 0; i < 个数; i = i + 1 {
total = total + 参数[i];
}
返 total;
}

函 主 () -> 整数 {
x 为 整数 求和(3, 10, 20, 30);
返 0;
}

```

- `...` 表示后面可跟任意个参数，必须在参数列表最后
- 函数体内 `参数` 是编译器自动提供的数组
- `参数[0]`、`参数[1]`……依次是第一个、第二个变长参数
- 类型统一为 `整数`
- 调用时，多余的实参自动打包成数组传入

**函数块分类**：

函数按所处上下文分为三类：

| 类别 | 定义 | 汇编符号 |
|---|---|---|
| **主函数类** | 名字为 `主` 或 `main` 的函数 | `main` |
| **模块函数类** | `#导入<file 模块名>` 引入的模块文件中的函数 | `模块名_函数名` |
| **函数类** | 主文件中定义的普通函数 | `函数名` |

**同名规则：**

- 同一类内不能同名
- **跨类可以同名**，互不冲突
- 模块函数按模块隔离：`math` 里的 `加` 和 `string` 里的 `加` 不冲突

**调用解析：**

```
调用 数学 {
    设 x 为 整数 加(1, 2);    _优先查 math 模块的 加_
}

设 y 为 整数 加(3, 4);        _查主文件的 加（主函数类 + 函数类）_
```

**模块内部互调：** 模块函数调其他模块函数，也走本模块命名空间。

**主函数唯一性：** 每个程序有且只有一个主函数。模块文件里即使有 `主` 也忽略（不当入口）。

---

## 十、块

两种风格，**同一块内不混用**：

**A. `{}`**

```
若 a > b {
    打印("a 大");
}
```

**B. `开始/结束` / `begin/end`**

```
若 a > b
开始
    打印("a 大");
结束
```

嵌套时内外可不同。

---

## 十一、控制流

```
若 条件 {
    ...
} 否则若 条件 {
    ...
} 否则 {
    ...
}

当 条件 {
    ...
}

循环 初始化; 条件; 步进 {
    ...
}
```

| 中 | 英 |
|---|---|
| 若 | if |
| 否则若 | elif |
| 否则 | else |
| 当 | while |
| 循环 | for |
| 跳 | break |
| 续 | continue |
| 返 | return |
| 匹配 | match |
| 情形 | case |
| 默认 | default |
| 落 | fallthrough |
| 转 | goto |

---

## 十二、作用域

- **函数外默认全局**
- **函数内默认局部**

```
a 为 整数 5;              _全局_
函 f() {
    b 为 全局 整数 10;   _全局_
    c 为  整数 20;        _局部_
}
```

**静态**：

```
a 为 静态 整数 5;         _函数结束后值还在_
```

---

## 十三、常量

```
PI 常量 双精 3.14159;
```

- 语法：`常量名 常量 类型 值`
- 值不能为 ∅
- 可以修改值、改类型：

```
修改 PI 常量 3.14 为 3.15;          _改值_
修改 PI 常量 3.14 为 双精 3.14159;   _改值+改类型_
```

- 不能改成变量：`修改 PI 为 3.14;` 非法

---

## 十四、指针

```
p 为 整数 指针 ∅;
p = &a;
*p = 20;
p->x;
```

**通过指针改变量**：

| 中文 | 英文 |
|---|---|
| `p 修改 a 为 100;` | `*p = a to 100;` |

---

## 十五、赋值

| 情况 | 中文 | 英文 |
|---|---|---|
| 声明+首次赋值 | `a 为 整数 5;` | `a is int 5;` |
| 改值（类型不变） | `修改 a 为 10;` | `a to 10;` |
| 改值+改类型 | `修改 a 整数 5 为 双精 3.82;` | `a int 5 to double 3.82;` |
| 只改类型 | `修改 a 为 双精;` | `a to double;` |

---

## 十六、类型转换

| 中文 | 英文 |
|---|---|
| `双精 3.14 转化为 整数` | `double 3.14 to int` |

---

## 十七、运算符

和 C 一致，除了：

| 项 | C | WELS-C |
|---|---|---|
| 不等于 | `!=` | `\=` |
| 幂 | 无 | `**` |
| 三路比较 | 无 | 不要 |

**完整表**：

- 算术：`+ - * / % **`
- 比较：`== \= < > <= >=`
- 逻辑：`&& || !`
- 位：`& | ^ ~ << >>`
- 赋值：`= += -= *= /= %= &= |= ^= <<= >>=`
- 自增自减：`++ --`
- 三元：`? :`
- 逗号：`,`
- 成员：`. ->`
- 下标：`[]`
- 指针：`&` 取地址，`*` 解引用
- 调用：`f(a, b)`

**`sizeof` / `大小`**：

```
大小(整数)         _8_
大小(字节)         _1_
大小(双精)         _8_
```

---

## 十八、注释

**单行**：

```
_注释_ 这是单行注释
```

**多行**：

```
_/
这是多行
可以写好几行
/_
```

---

## 十九、字符串

**字符串 = 从 `"` 开始，到下一个"后面紧跟 `,` / `)` / `;` / 空白 / 换行 / `\`"的 `"` 为止，内容原样保留。**

**字符串内不做任何转义** —— `\`、`/`、`"` 等字符直接写。

**`\n` 和 `\t` 写在字符串外面**，作为独立的字符 token。

### 示例

```
打印("084"0"");          _输出 084"0"_
打印("第一行"\n);        _输出 第一行 + 换行_
打印("a", b, "c");       _三个参数：a、b、c_
```

### 转义字符

字符串外**只允许** `\n` 和 `\t`：

| 写法 | 含义 |
|---|---|
| `\n` | 换行 |
| `\t` | 制表 |

其他转义（`\r`、`\\`、`\"` 等）一律**语法错误**（`\=` 运算符除外）。

### 多参数

`打印` 支持多个参数，逗号分隔：

```
打印("a = ", a, \n);
```

- 字符串参数按内容原样输出
- 整数参数按十进制输出
- 浮点参数按 `%f` 输出
- `\n`、`\t` 作为独立参数，对应换行符 / 制表符

---

## 二十、字面量

**数字**：

| 进制 | 写法 | 默认 |
|---|---|---|
| 十进制 | `100D` | 不加后缀就是十进制 |
| 二进制 | `1010B` | |
| 十六进制 | `FFFFH` 或 `0xFFFF` | |

**浮点**：

```
3.14
1e10
1.5e-3
```

**字符**：

```
'a'
```

**布尔**：

```
真
假
```

---

## 二十一、预编译

| 功能 | 中 | 英 |
|---|---|---|
| 引入头文件 | `#<stdio.weh>` | 同 |
| 导入模块 | `#导入<utils/math.wec 数学>` | `#import<utils/math.wec math>` |
| 定义宏 | `#定宏 MAX 100` | `#define MAX 100` |
| 取消宏 | `#消宏 MAX` | `#undef MAX` |
| 若定义 | `#若定义 DEBUG` | `#ifdef DEBUG` |
| 若未定 | `#若未定 DEBUG` | `#ifndef DEBUG` |
| 否则 | `#否则` | `#else` |
| 否则若 | `#否则若` | `#elif` |
| 结束若 | `#结束若` | `#endif` |
| 编译提示 | `#编示 once` | `#pragma once` |

**外部声明（跨编译单元）：**

外部变量：

```

外来 全局计数器 为 整数;

```

外部函数：

```

外来 函 wels_print (s: 字符串) -> 空;
外来 函 wels_print_int (n: 长整) -> 空;

```

- 声明一个在别的文件（或 runtime）定义的全局变量 / 函数
- 本文件不生成符号，符号由链接器解析
- 用于多文件项目共享全局状态、或调用运行时 / 系统接口

---

## 二十二、模块

**导入**：

```
#导入<utils/math.wec 数学>
```

- 相对路径 + 文件名（必须带 `.wec` 后缀）+ 模块名

**使用**：

```
调用 数学 {
    设 z 为 整数 100;
    设 w 为 整数 加(1, 2);
}

打印(z);        _100_
打印(w);        _3_
```

- `调用 模块名 { ... }` 块内无限制
- 模块内的名字在块内优先
- 块内声明的变量，块外可见

**编译单元隔离**：

`#导入<file 模块名>` **不进行纯文本包含**。编译器按独立编译单元处理：

1. 扫描所有 `#导入` 指令，建立模块清单 `[(path, name), ...]`
2. 逐个解析模块文件，收集函数签名到"模块表"
3. 解析主文件，遇到函数调用按当前上下文查表
4. codegen 阶段，模块函数符号加前缀 `模块名_`

**模块间可见性：**

- 模块函数**只在** `调用 模块名 { ... }` 块内可见
- 模块内函数互调时，可见本模块所有函数
- 模块函数**不被主文件其他位置看见**（避免误调）
- 主文件的函数**不被模块函数看见**（模块独立）

**模块文件后缀：** 模块文件建议用 `.weh` 后缀（头文件），主文件用 `.wec`。后缀不强制，但推荐。

---

## 二十三、内联汇编

```
汇编 {
    "cli";
    "mov %cr3, %rax";
}
```

（`设` 和 `let` 是同一关键字的两种写法，见"预编译"节）

引号包围，和 C 一致。

---

## 二十四、符号

**语法符号全 ASCII**，字面量可用 Unicode（如 `∞`）。

**中文全角符号做语法符号一律非法。**

---

## 二十五、路径

- 盘符：`C:` `D:` `E:`
- 大小写不敏感
- 斜杠模式：`C://docs/hello.txt`
- 反斜杠模式：`C:\docs\hello.txt` 或 `C:docs\hello.txt`
- 单斜杠 `C:/` 非法
- 一个路径里不混用 `/` 和 `\`
- 路径大小写敏感

---

## 二十六、与 C 的差异总表

| 项 | C | WELS-C |
|---|---|---|
| 声明 | `int a = 5;` | `a 为 整数 5;` |
| 类型位置 | 前 | 后（修饰符跟类型） |
| 不等于 | `!=` | `\=` |
| 幂 | 无 | `**` |
| 结构字段 | `;` 分隔 | 换行分隔 |
| 结构变量 | 单独声明 | 结构内声明 |
| 块 | 只有 `{}` | `{}` 或 `begin/end` |
| 头文件 | `#include` | `#<file.weh>` |
| 模块 | 无 | `#导入<>` + `调用` |
| 注释 | `//` `/* */` | `_注释_` `_/ /_` |
| 字符串转义 | 字符串内 | `\n` 在字符串外 |
| 容器 | 无 | 有 |
| 区间 | 无 | 有 |
| 常量修改 | 不可改 | 可改（特殊语法） |
| 前置声明 | 需要 | 不需要 |
| 默认参数 | 无 | 无 |
| 函数重载 | 无 | 无 |
| 变长参数 | `...` | `...` |

