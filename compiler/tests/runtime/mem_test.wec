#<io.weh>
#<mem.weh>

函 主 () -> 整数 {
    打印行("初始剩余:");
    打印整数(剩余内存());
    打印行("");

    p 为 空 指针 分配(100);
    打印行("分配 100 后剩余:");
    打印整数(剩余内存());
    打印行("");

    q 为 空 指针 分配(1000);
    打印行("再分配 1000 后剩余:");
    打印整数(剩余内存());
    打印行("");

    返 0;
}
