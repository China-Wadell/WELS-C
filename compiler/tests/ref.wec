函 主 () -> 整数 {
    a 为 整数 10;
    b 为 引用 a;
    打印("b = ", b, \n);
    修改 b 为 20;
    打印("a = ", a, \n);
    修改 a 为 30;
    打印("b = ", b, \n);
    若 a == 30 && b == 30 {
        打印("引用通过"\n);
    }
    返 0;
}
