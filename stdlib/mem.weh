_注释_ mem.weh — WELS-C 内存库

外来 函 wels_malloc (n: 长整) -> 空 指针;
外来 函 wels_free (p: 空 指针) -> 空;
外来 函 wels_heap_remaining () -> 长整;

函 分配 (n: 长整) -> 空 指针 {
    返 wels_malloc(n);
}

函 释放 (p: 空 指针) -> 空 {
    wels_free(p);
}

函 剩余内存 () -> 长整 {
    返 wels_heap_remaining();
}

_注释_ 内存复制：把 src 的 n 字节复制到 dst_
函 内存复制 (dst: 字符 指针, src: 字符 指针, n: 长整) -> 空 指针 {
    i 为 长整 0;
    循环 i 为 长整 0; i < n; i = i + 1 {
        dst[i] = src[i];
    }
    返 dst;
}

_注释_ 内存填充：把 p 的 n 字节填成值 v_
函 内存填充 (p: 字符 指针, v: 整数, n: 长整) -> 空 指针 {
    i 为 长整 0;
    循环 i 为 长整 0; i < n; i = i + 1 {
        p[i] = v;
    }
    返 p;
}

_注释_ 内存比较：a == b 返 0；a < b 返负数；a > b 返正数_
函 内存比较 (a: 字符 指针, b: 字符 指针, n: 长整) -> 整数 {
    i 为 长整 0;
    循环 i 为 长整 0; i < n; i = i + 1 {
        若 a[i] \= b[i] {
            返 a[i] - b[i];
        }
    }
    返 0;
}

_注释_ 清零：把 p 的 n 字节填 0_
函 清零 (p: 字符 指针, n: 长整) -> 空 指针 {
    返 内存填充(p, 0, n);
}
