函 主 () -> 整数 {
    a 为 整数 5;
    b 为 整数 10;
    max 为 整数 a > b ? a : b;
    打印("max = ", max, \n);
    min 为 整数 a < b ? a : b;
    打印("min = ", min, \n);
    嵌套 为 整数 a > b ? 100 : a < b ? 200 : 300;
    打印("嵌套 = ", 嵌套, \n);
    返 0;
}
